*.env
*.ovpn
.vagrant

# Documentation is committed encrypted (see .sops.yaml); plaintext stays local.
docker-opensearch/SECURITY-CONFIG.md
docker-vault-transit/UNSEAL.md

# Runbooks are committed encrypted (see .sops.yaml); plaintext stays local.
docs/**/*.md
